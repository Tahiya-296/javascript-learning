//3. Use filter to keep only names longer than 4 characters from an array of names. 

const name = ["Alvarez", "Messi", "Romero", "Lily"];

const printName = name.filter((name) => name.length > 4);

console.log(printName);
