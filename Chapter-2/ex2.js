//2. Predict the output of console.log(typeof null) — then run it and search the web for why the  answer is surprising. 
 console.log(typeof null).
   
//output=object
//typeof null` returns `"object"` because of an old bug in JavaScript that was never fixed for compatibility reasons.
//when JavaScript was first created, null was accidentally classified as an object internally.
   
